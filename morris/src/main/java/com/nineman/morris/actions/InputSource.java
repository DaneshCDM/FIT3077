package com.nineman.morris.actions;

/**
 * Interface representing an input source for the Nine Men's Morris game actions.
 */
public interface InputSource {
    /**
     * Gets the input from the input source.
     * @return The input as a string.
     */
    String getInput();
}
